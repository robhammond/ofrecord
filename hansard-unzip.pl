#!/usr/bin/env perl
use strict;
use warnings;
use Modern::Perl;
use Mojo::UserAgent;

# loop thru zip files
for (my $i = 1; $i <= 424; $i++) {
	# unzip & parse xml file
	# insert into ES database
	# delete xml file
}